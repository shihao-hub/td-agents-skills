# GUIDE-AGY-CHAT-FEATURES：Antigravity 对话输入功能指南

## 一、斜杠命令（Slash Commands）

Google Antigravity 中的**斜杠命令（Slash Commands）**是一套快捷工作流：在聊天输入框中输入 `/` 即可调出该菜单，用于快速触发特定模式或启动专属子智能体。

以下为您逐一详细介绍每个功能的核心定位、工作机制与典型使用场景：

### 1. `/goal` —— 目标达成模式（自治托管）

- **官方释义**：*Run until the specified goal is completely finished.*
- **核心定位**：让智能体进入**闭环自治运行状态**，直到完全达成指定目标才结束，期间会自动排错、自查验证，不会中途轻易停下。
- **工作机制**：智能体会将目标拆解为执行循环（执行 → 运行测试/检查 → 发现问题 → 自行修复 → 再次验证），反复迭代直到验收达标。
- **适用场景**：
  - 适合耗时较长、可以托管的任务（例如重构某个完整模块、编写整套单元测试并全部跑通、批量修补 Lint 报错）。
  - “交待完任务后去喝杯咖啡/通宵运行”，让智能体自主搞定。

### 2. `/schedule` —— 定时与调度任务

- **官方释义**：*Run an instruction on a recurring schedule or as a one-time timer.*
- **核心定位**：用于创建**单次延时提醒**（One-shot Timer）或**周期性定时任务**（Cron Job）。
- **工作机制**：在后台注册调度器，到达指定时间或间隔时，自动唤醒智能体执行指令或向你发送通知。
- **适用场景**：
  - **延时提醒**：如“10 分钟后检查 CI 构建或服务部署状态”。
  - **周期轮询**：如“每小时拉取一次线上错误日志并做摘要”、“每天早上生成今日日程与待办清单”。

### 3. `/browser` —— 浏览器智能体（网页交互）

- **官方释义**：*Invoke a browser agent for web tasks.*
- **核心定位**：启动拥有**真实浏览器控制权限**的独立 Agent，执行复杂的 Web 交互。
- **工作机制**：驱动浏览器内核打开页面、处理 JavaScript 动态渲染、模拟点击、输入表单、截屏审查、下载资源或进行 UI 验证。
- **适用场景**：
  - 查阅需要动态加载或交互的在线文档/控制台。
  - 进行 Web 应用的端到端（E2E）UI 流程测试。
  - 需要在浏览器中观察实际渲染效果并排查前端问题的场景。

### 4. `/plan` —— 慎密规划模式（先谋后动）

- **官方释义**：*Plan carefully before executing a task.*
- **核心定位**：**先规划审阅，后动手写代码**。防止智能体在没有充分理解上下文时直接盲改项目。
- **工作机制**：智能体只读扫描相关代码、分析调用链与架构模式，输出一份包含“需求理解 → 方案设计 → 分步任务拆解 → 验证方法”的完整计划，等待你审阅批准后才开始修改文件。
- **适用场景**：
  - 中大型功能开发、多文件协作修改或核心架构重构。
  - 对代码变更有高审查要求，希望严格把控实现步骤的研发任务。

### 5. `/grill-me` —— 方案对齐访谈（“拷问”模式）

- **官方释义**：*Interview me to align on a plan.*
- **核心定位**：**通过主动提问帮助用户梳理和敲定需求**。
- **工作机制**：智能体不会急着写代码或给出单向方案，而是扮演资深架构师/技术面试官，向你抛出一系列关键问题（边界条件、异常处理、架构权衡、技术选型等），逼近需求的真实全貌。
- **适用场景**：
  - 刚有一个初步想法，但细节尚未明确或潜在隐患未知。
  - 复杂系统设计前夕，想通过多轮一问一答消除设计盲区、达成共识。

### 6. `/teamwork-preview` —— 多智能体协同预览

- **官方释义**：*Invoke a team of agents to autonomously tackle large projects.*
- **核心定位**：启动**多智能体团队（Agent Team）**协作攻坚大型复杂项目。
- **工作机制**：将一个大型工程拆分给具有不同专业角色的子 Agent（如架构分析、并发模块编码、集成测试、代码审查等），多 Agent 并行或按依赖顺序分工推进。
- **适用场景**：
  - 超大型全栈项目、跨多个子系统的协同开发。
  - 单个会话上下文不足以承载海量文件与任务时的并行化开发。

### 7. `/learn` —— 经验总结与沉淀

- **官方释义**：*Reflect on recent successes or corrections to capture reusable skills or rules.*
- **核心定位**：**将单次经验固化为可持续复用的资产**。
- **工作机制**：反思当前会话中遇到的曲折、踩坑排错、用户的正向纠偏或成功经验，将其提取为项目规则（Rules）或自定义技能（Skills）保存在本地。
- **适用场景**：
  - 刚刚解决了一个极其棘手的配置/环境报错，希望智能体“永远记住这个经验，以后不要再犯”。
  - 纠正了智能体的某项编码偏好或规范，希望沉淀为项目通用的开发准则。

### 8. `/boost` —— 深度思考增强调度器

- **官方释义**：*Invoke the Boost multi-agent orchestrator for complex tasks.*
- **核心定位**：面向**高难度、严谨性要求极高**的任务启动最高强度的编排引擎。
- **工作机制**：结合深度思考（Deep Thinking）、战略规划、多视角审查（例如对抗式审查/Hostile Review）与严苛的多阶段验证，投入更多算力保障输出质量。
- **适用场景**：
  - 复杂并发/死锁排查、核心算法实现、大规模底层重构等容错率极低的场景。
  - 普通提问难以一次性做对的复杂科研或疑难编码难题。

### 💡 快速选择速查表

| 当前需求 / 状态 | 推荐使用的命令 |
| :--- | :--- |
| **只有一个初步想法，还没想透** | `/grill-me`（先让 AI 提问理清思路） |
| **需求明确，但涉及较多代码改动** | `/plan`（先出计划，确认无误再动手） |
| **需要自动化长耗时跑完某件事** | `/goal`（全自动循环，直到完全完成） |
| **遇到了特别硬核、容错率低的难题** | `/boost`（多视角深度推演与严格验证） |
| **需要操作网页、抓取动态页或 UI 测试** | `/browser`（打开浏览器交互） |
| **想隔一段时间执行或周期性监控** | `/schedule`（配置定时器或 Cron） |
| **刚刚踩坑排错完毕，不想以后再犯** | `/learn`（提取规则与技能沉淀下来） |
| **超大工程需要多人分工式的大模型协作** | `/teamwork-preview`（多 Agent 团队并行） |

---

## 二、Add Context（添加上下文）菜单

**“Add Context（添加上下文）”**是 Antigravity 对话输入框中的菜单（通常点击输入框左侧的 `+` 图标弹出）。

它的核心作用是：**在发送提示词时，精准、结构化地为智能体注入多维度的上下文信息或外部环境联动**，让智能体在充分了解现场情况的前提下给出高质量解答，避免因信息缺失而产生幻觉。

以下是该菜单中各功能的详细介绍：

### 1. 🖼️ Media（媒体文件 / 图片）

- **核心功能**：向当前对话上传本地图像或视觉资源。
- **注入的内容**：本地截图、UI 原型设计图（Figma / 效果图）、系统架构草图、控制台或软件弹出的报错截图等。
- **智能体如何处理**：利用 Gemini 多模态视觉理解能力，直接识别图像中的文字、UI 布局、图表逻辑或错误堆栈。
- **典型场景**：
  - 遇到前端样式跑偏时，上传界面截图并询问：“如何调整 CSS 还原此效果？”
  - 遇到难以复制纯文本的系统弹窗报错时，截屏直接发给 AI 排查。
  - 上传手绘草图或流程图，让 AI 直接生成对应的代码或 Mermaid 架构图。
- **快捷技巧**：除了点击此项，也可以直接在输入框中按 `Ctrl + V` 粘贴剪贴板截图，或将图片拖拽到聊天区域。

### 2. @ Mentions（引用实体与上下文）

- **核心功能**：唤起 `@` 引用面板，将项目中的具体资源精确绑定到当前问题中。
- **支持引用的对象类型**：
  - **Files & Folders（文件与目录）**：精准指定某个文件或目录（如 `@src/api/auth.ts`、`@components/`），智能体会优先读取其内容。
  - **Code Symbols（代码符号）**：直接引用具体的类名、函数名或接口（如 `@UserService`、`@calculateTax`）。
  - **Conversations（历史对话）**：引用过去的某次会话记录，实现跨会话的上下文衔接与经验复用。
  - **Terminals / Background Tasks（终端/后台任务）**：绑定某个正在运行的终端会话或后台任务输出日志。
  - **Rules / MCP Servers**：引用本地定制的规则文件或外部 MCP 工具端点。
- **典型场景**：
  - 避免在提示词中打冗长的相对路径，直接输入 `@` 模糊搜索并挂载文件。
  - 确保智能体只聚焦于关键文件，避免读取全仓造成上下文污染和 Token 浪费。
- **快捷技巧**：在输入框内任何位置直接键入 `@` 符号，即可随时唤出引用菜单。

### 3. 📝 Actions（快捷动作 / 预设指令）

- **核心功能**：提供针对当前活动代码或编辑器状态的**常用预设操作模板**。
- **常见动作类型**：
  - **Explain Code（解释代码）**：深入剖析当前选区或文件的核心逻辑与实现机制。
  - **Refactor（重构代码）**：在保持现有行为的前提下，提取函数、优化可读性与性能。
  - **Write Tests（编写测试）**：为当前选中的函数或模块自动生成单元测试用例。
  - **Fix / Auto-Fix（诊断修复）**：结合编辑器当前的 Lint 警告或编译器报错，自动生成修复补丁。
  - **Add Comments / Docstrings（补充注释）**：为接口和复杂算法补全规范的代码注释文档。
- **典型场景**：
  - 不需要手动输入一长串提示词（如“请帮我为这个类写一套 Jest 单元测试”），点击对应 Action 即可一键生成标准指令。

### 4. 🌐 Browser（浏览器会话 / 网页上下文联动）

- **核心功能**：将**外部浏览器页面或活跃标签页（Active Tab）**的实时状态捕获并作为上下文注入。
- **注入的内容**：
  - 当前浏览页面的 DOM 结构与文本正文。
  - 网页控制台（Console）的报错信息与网络请求（Network）状态。
  - 当前页面的可视区域渲染截图。
- **典型场景**：
  - **前端与全栈联调**：本地运行前端项目时，直接引入浏览器页面上下文，让 AI 比对代码与渲染效果。
  - **第三方文档与控制台**：在浏览器中打开了某云厂商控制台或 API 文档，通过此项让 AI 直接读取当前页面内容协助配置。
  - 配合 `/browser` 智能体完成自动化的页面交互测试与数据验证。

### 💡 组合使用最佳实践

这些上下文入口可以**多路混合搭配**，实现最佳的提示词质量。例如：

> **实战示例**：
> 1. 点击 `Media` 上传一张 **UI 设计稿截图**；
> 2. 键入 `@Mentions` 挂载对应的 **`@UserProfile.tsx`**；
> 3. 输入提示词：“*请根据设计图调整此组件的排版和间距，补齐缺少的徽章元素。*”

这样智能体既有**视觉目标（Media）**，又有**代码基线（Mentions）**，输出的代码准确率会远高于纯文字描述。
